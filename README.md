Practicing the combination of Playwright with Python + Behave framework.

Quick installation and setup:
- `pip install -r requirements.txt`
- `playwright install`
- `Gherkin` plugin by JetBrains is extremely helpful, but not mandatory.

Run tests:
- `behave /features`